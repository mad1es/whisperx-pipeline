# Мультимодальный анализ речи для выявления суицидального риска

Полноценный пайплайн для обработки видео/аудио данных с извлечением мультимодальных признаков (аудио + текст) для анализа паттернов, отличающих людей с суицидальными намерениями от контрольной группы.

## 📋 Описание проекта

Проект предназначен для автоматического извлечения и анализа признаков из видео записей с целью выявления акустических и лингвистических паттернов, связанных с суицидальным риском.

### Основные возможности

- **🚀 Полный автоматический пайплайн** - одна команда для обработки всех данных
- **Извлечение аудио** из видео файлов с нормализацией
- **Транскрипция** с точными таймстемпами на уровне слов (WhisperX)
- **Сегментация** аудио на фразы с выравниванием по транскрипциям
- **Извлечение акустических признаков** через openSMILE (eGeMAPSv02)
- **Объединение признаков** в единый датасет
- **Базовые ML модели** для классификации
- **Интерактивная визуализация** для исследования данных

## 🏗️ Структура проекта

```
opensmile_with_mywhisper/
├── data/
│   ├── raw_videos/          # Исходные видео файлы
│   ├── audio_wav/           # Извлеченное аудио (WAV)
│   ├── transcripts/         # JSON транскрипции WhisperX
│   ├── segments/            # Сегментированное аудио
│   ├── features/            # Извлеченные признаки
│   ├── models/              # Обученные модели
│   └── results/             # Результаты и графики
├── pipeline/
│   ├── extract_audio.py              # Извлечение аудио из видео
│   ├── transcribe_whisperx.py        # Транскрипция с WhisperX
│   ├── segment_audio.py               # Сегментация аудио
│   ├── extract_opensmile_features.py  # Извлечение признаков openSMILE
│   ├── merge_features.py              # Объединение признаков
│   ├── train_baseline.py              # Обучение базовой модели
│   └── run_full_pipeline.py           # 🚀 Автоматический запуск всего пайплайна
├── visualization_app/
│   └── app.py               # Streamlit приложение для визуализации
├── opensmile/               # openSMILE библиотека
├── config/                  # Конфигурационные файлы
├── requirements.txt         # Python зависимости
└── README.md               # Документация
```

## 🚀 Установка

### 1. Клонирование и настройка окружения

```bash
# Создание виртуального окружения
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установка зависимостей
pip install -r requirements.txt
```

### 2. Установка FFmpeg

**macOS:**
```bash
brew install ffmpeg
```

**Linux:**
```bash
sudo apt-get install ffmpeg
```

**Windows:**
Скачайте с [официального сайта](https://ffmpeg.org/download.html) и добавьте в PATH.

### 3. Проверка openSMILE

Убедитесь, что openSMILE собран и бинарник доступен:
```bash
./opensmile/build/progsrc/smilextract/SMILExtract -h
```

Если бинарник не найден, соберите openSMILE:
```bash
cd opensmile
mkdir build && cd build
cmake ..
make
```

### 4. Подготовка данных

Создайте файл `data/metadata.csv` с метаданными видео:
```csv
id,label
001_2025-01-27,0
002_2025-01-28,1
...
```

Где `label`:
- `0` - контрольная группа
- `1` - группа с суицидальными намерениями

**Важно:** `id` должен совпадать с именем файла без расширения (например, `001_2025-01-27` для `001_2025-01-27.mp4`).

Поместите видео файлы в `data/raw_videos/`.

**Добавление новых видео:** При добавлении новых видео вы можете указать в `metadata.csv` только новые видео — система автоматически сохранит метаданные старых видео. Подробнее см. `HOW_TO_ADD_VIDEOS.md`.

## 📖 Использование

### 🎬 Обработка одного видео (быстрый тест)

Если вы хотите протестировать пайплайн на одном видео и посмотреть результаты:

1. **Поместите одно видео** в `data/raw_videos/` (например, `001_2025-01-27.mp4`)

2. **Создайте `data/metadata.csv`** с одной строкой:
   ```csv
   id,label
   001_2025-01-27,0
   ```
   (где `id` - имя файла без расширения, `label` - 0 или 1)

3. **Запустите полный пайплайн:**
   ```bash
   python pipeline/run_full_pipeline.py
   ```

4. **После завершения** (может занять 5-15 минут):
   - Признаки будут в `data/features/merged_features.csv`
   - Графики модели в `data/results/` (если обучение не пропущено)
   - Запустите визуализацию:
     ```bash
     streamlit run visualization_app/app.py
     ```

**Совет:** Если хотите только извлечь признаки без обучения модели:
```bash
python pipeline/run_full_pipeline.py --skip-training
```

---

### 🚀 Быстрый старт: Полный автоматический пайплайн

**Самый простой способ** - запустить весь пайплайн одной командой:

```bash
python pipeline/run_full_pipeline.py
```

Этот скрипт автоматически выполнит все этапы:
1. ✅ Извлечение аудио из видео
2. ✅ Транскрипция с WhisperX
3. ✅ Сегментация аудио
4. ✅ Извлечение признаков openSMILE
5. ✅ Объединение признаков
6. ✅ Обучение базовой модели

**Параметры запуска:**

```bash
# С GPU ускорением для транскрипции
python pipeline/run_full_pipeline.py --whisper-device cuda

# Пропустить определенные этапы (если уже выполнены)
python pipeline/run_full_pipeline.py --skip-audio --skip-transcription

# Использовать другую модель Whisper
python pipeline/run_full_pipeline.py --whisper-model large --whisper-language ru
```

**Доступные опции:**
- `--skip-audio` - пропустить извлечение аудио
- `--skip-transcription` - пропустить транскрипцию
- `--skip-segmentation` - пропустить сегментацию
- `--skip-features` - пропустить извлечение признаков
- `--skip-merge` - пропустить объединение признаков
- `--skip-training` - пропустить обучение ML модели классификации (см. раздел "Результаты ML модели")
- `--whisper-model` - модель Whisper (tiny/base/small/medium/large)
- `--whisper-language` - язык (ru/en и т.д.)
- `--whisper-device` - устройство (cpu/cuda)

**Примечание:** `--skip-training` пропускает обучение классификатора (Logistic Regression / Random Forest / Gradient Boosting), который предсказывает суицидальный риск на основе извлеченных признаков. Если вы хотите только извлечь признаки без обучения модели, используйте эту опцию.

---

### 📝 Пошаговая обработка (ручной режим)

Если нужно запустить этапы по отдельности:

### Шаг 1: Извлечение аудио

```bash
python pipeline/extract_audio.py \
    --input-dir data/raw_videos \
    --output-dir data/audio_wav \
    --sample-rate 16000
```

### Шаг 2: Транскрипция

```bash
python pipeline/transcribe_whisperx.py \
    --input-dir data/audio_wav \
    --output-dir data/transcripts \
    --model medium \
    --language ru \
    --device cpu
```

**Примечание:** Для GPU ускорения используйте `--device cuda` (требуется CUDA).

### Шаг 3: Сегментация аудио

```bash
python pipeline/segment_audio.py \
    --audio-dir data/audio_wav \
    --transcript-dir data/transcripts \
    --output-dir data/segments \
    --min-duration 2.0 \
    --max-duration 5.0
```

### Шаг 4: Извлечение признаков openSMILE

```bash
python pipeline/extract_opensmile_features.py \
    --segments-dir data/segments \
    --output-dir data/features \
    --opensmile-dir opensmile \
    --output-csv data/features/opensmile_features.csv
```

### Шаг 5: Объединение признаков

```bash
python pipeline/merge_features.py \
    --segments-metadata data/segments/segments_metadata.csv \
    --opensmile-features data/features/opensmile_features.csv \
    --metadata data/metadata.csv \
    --output data/features/merged_features.csv \
    --language ru \
    --aggregate
```

### Шаг 6: Обучение базовой модели

```bash
python pipeline/train_baseline.py \
    --data data/features/merged_features.csv \
    --model-type logistic \
    --n-splits 5 \
    --output-dir data/models \
    --results-dir data/results
```

### Шаг 7: Интерактивная визуализация

```bash
streamlit run visualization_app/app.py
```

Приложение откроется в браузере по адресу `http://localhost:8501`.

## 🔧 Конфигурация

### Параметры WhisperX

- **Модель:** `tiny`, `base`, `small`, `medium`, `large`
- **Язык:** `ru` (русский), `en` (английский) и др.
- **Устройство:** `cpu` или `cuda`

### Параметры сегментации

- **Минимальная длительность:** 2.0 секунды (рекомендуется)
- **Максимальная длительность:** 5.0 секунд (рекомендуется)

### Параметры openSMILE

Используется конфигурация **eGeMAPSv02** (Geneva Minimalistic Acoustic Parameter Set), которая включает:
- Pitch (F0) features
- Energy/Loudness features
- Spectral features
- Voice quality features (jitter, shimmer, HNR)
- MFCC features

## 📊 Выходные данные

### merged_features.csv

Объединенный датасет с признаками на уровне сегментов:
- `file_id` - ID видео
- `segment_id` - ID сегмента
- `start`, `end` - временные метки
- `text` - транскрипция
- `asr_conf_mean`, `asr_conf_std` - статистики ASR confidence
- `speech_rate` - скорость речи
- Акустические признаки openSMILE (F0_mean, energy_mean, jitter, shimmer, MFCC и т.д.)
- Текстовые признаки (pronoun_count, negation_count, type_token_ratio)

### features_per_video.csv

Агрегированные признаки на уровне видео (mean/std/min/max/median по всем сегментам).

## 🎯 Результаты ML модели

### Что делает модель?

Модель обучается для **бинарной классификации** сегментов речи:
- **Класс 0** - контрольная группа (обычные люди)
- **Класс 1** - группа с суицидальными намерениями

Модель использует **все мультимодальные признаки**:
- Акустические признаки openSMILE (pitch, energy, jitter, shimmer, MFCC и т.д.)
- Текстовые признаки (speech_rate, pronoun_count, negation_count, type_token_ratio)
- Статистики ASR confidence

### Доступные модели

1. **`logistic`** (по умолчанию) - Logistic Regression
   - Быстрая линейная модель
   - Хорошо работает для базовой оценки
   - Параметры: `C=0.5`, `class_weight='balanced'`

2. **`rf`** - Random Forest
   - Ансамбль деревьев решений
   - Может уловить нелинейные зависимости
   - Параметры: `n_estimators=100`, `max_depth=10`

3. **`gbm`** - Gradient Boosting
   - Бустинг деревьев
   - Обычно лучшая точность
   - Параметры: `n_estimators=100`, `max_depth=5`, `learning_rate=0.1`

### Валидация

Используется **групповая стратифицированная кросс-валидация** (StratifiedGroupKFold):
- 5 фолдов по умолчанию
- Сегменты одного видео не попадают одновременно в train и test
- Это предотвращает утечку данных между видео одного человека

### Метрики

Модель оценивается по:
- **ROC-AUC** - площадь под ROC кривой
- **PR-AUC** - площадь под Precision-Recall кривой
- **Оптимальный порог** - находится для Precision >= 0.9 (максимизирует Recall)

### Выходные файлы

После обучения модели создаются:
- `baseline_{model_type}.pkl` - сохраненная модель + scaler + threshold
- `feature_importance.csv` - топ-20 самых важных признаков
- `curves.png` - ROC и PR кривые
- `confusion_matrix.png` - матрица ошибок с оптимальным порогом

## 🔍 Интерактивная визуализация

Streamlit приложение предоставляет:

1. **Обзор** - общая статистика по датасету
2. **Распределения** - сравнение признаков между группами
3. **Временная шкала** - визуализация речи с pitch, energy, confidence
4. **Корреляции** - тепловая карта корреляций между признаками
5. **PCA/Кластеризация** - визуализация в пространстве признаков

## 📝 Примечания

- Убедитесь, что у вас достаточно места на диске (обработка видео требует много места)
- Транскрипция с WhisperX может занять много времени (особенно на CPU)
- Для больших датасетов рекомендуется использовать GPU для ускорения
- openSMILE должен быть собран и доступен в системе

## 🐛 Решение проблем

### Ошибка "SMILExtract не найден"
Убедитесь, что openSMILE собран:
```bash
cd opensmile/build
make
```

### Ошибка "FFmpeg не найден"
Установите FFmpeg и добавьте в PATH.

### Ошибка при транскрипции
Проверьте, что модель WhisperX загружена правильно. При необходимости скачайте модель вручную.

### Недостаточно памяти
Уменьшите размер батча в `transcribe_whisperx.py` (параметр `batch_size`).

## 📚 Дополнительные ресурсы

- [openSMILE документация](https://www.audeering.com/opensmile/)
- [WhisperX документация](https://github.com/m-bain/whisperX)
- [eGeMAPSv02 описание](https://github.com/audeering/opensmile/blob/master/config/egemaps/v02/README.md)

## 📄 Лицензия

Проект использует openSMILE (лицензия см. `opensmile/LICENSE`).

## 👥 Авторы

Проект создан для исследования паттернов речи, связанных с суицидальным риском.

